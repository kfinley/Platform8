{
  "name": "@platform8/root",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "scripts": {
    "clean": "lerna clean --yes",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rkf",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
